paths:
  migrations: ./db/migrations
  seeds: ./db/seeds

environments:
  default_migration_table: phinxlog
  default_environment: development

  development:
    adapter: mysql
    host: estagio-teste-backend-mysql
    name: teste_backend_estagio
    user: root
    pass: "{coloque aqui sua senha}"
    port: 3306
    charset: utf8

templates:
  file: ./db/migrations/templates/template_migration.php.template

version_order: creation
